export { default as ModernLoadingSpinner } from './ModernLoadingSpinner';
export { default as ModernPageLoader } from './ModernPageLoader';

// Re-export the main loader for backward compatibility
export { default as ModernLoader } from '../ModernLoader';
